import SwiftUI

struct AppColors {
    static let background = Color(hex: "#452829")
    static let surface = Color(hex: "#57595B").opacity(0.55)
    static let card = Color(hex: "#57595B").opacity(0.75)
    static let textPrimary = Color(hex: "#F3E8DF")
    static let textSecondary = Color(hex: "#E8D1C5").opacity(0.9)
    static let accent = Color(hex: "#E8D1C5")
    static let accentSoft = Color(hex: "#F3E8DF").opacity(0.3)
    static let border = Color(hex: "#E8D1C5").opacity(0.15)
    static let destructive = Color(red: 0.85, green: 0.25, blue: 0.25)
    static let shadow = Color.black.opacity(0.45)
}
